/**
 * Just an example of a typical table in your db; customize
 * and create more as needed.
 */
interface ExampleTable {
    id: number
    someNumber: number
    someString: string
}

export {ExampleTable}